<!DOCTYPE html>
<html lang="es">
  <%-include ("../partials/head", {cssSheets: "allsTheShow", title: "Todos los Shows"}) %>
  <body>
    <%- include('../partials/header') %>
    <% shows.forEach(show => { %> 
      <section class="Shows">
      <article class="eventos-home">
       <figure class="Img-Artista"> <img
          src="/img/Img-Index-Artistas/<%= show.image %>"
          alt="Foto-Artista"
        /></figure>
        <div class="descripcion">
          
          <p class="nombre"><%= show.name %></p>
          <p class="fecha"><%= show.date %></p>
          <p class="Info">más info</p>
          <a href="/shows/detail/<%= show.id %>" class="boton-entrada">Compra tu entrada</a>
        </div>
        
      </article></section>
      <% }) %>
    
    <%- include('../partials/footer') %>
    <!-- Agregando archivo nav.js para el menu -->
    <script src="js/nav/nav.js"></script>