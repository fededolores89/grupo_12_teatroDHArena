<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ImageShow</title>
</head>
<body>
    <% if (shows.length > 0) { %>
        <% shows.forEach(image => { %>
            <figure class="Img-Artista">
                <img src="/img/Img-Index-Artistas/<%= image %>" alt="Foto-Artista"  />
            </figure>
        <% }) %>
    <% } else { %>
        <p>El artista no existe en la base de datos.</p>
    <% } %>
</body>
</html>